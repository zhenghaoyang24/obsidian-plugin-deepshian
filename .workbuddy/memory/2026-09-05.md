# 2026-09-05

- 通读全仓（src/ 15 个 ts、dsh-profile 桥接脚本、build 脚本、tests），建立项目架构认知；产出架构图向用户讲解。
- 关键结构记录已归档到 `MEMORY.md`（架构总览 + i18n/taskkill/懒创建会话等约定）。
- 分析「新对话/切换会话不终止进行中的任务」可行性：结论可行，且能完全用 dsh 原生能力实现。已通读 dsh 0.1.1-rc.2 源码核实（AgentRegistry 多 agent 并发、`agent.status` + `agent/status` 事件、`followup` 排队不打断、`session/event` 全局派发、`listSessions().live`）。阻塞点全在插件自身：bridge 的单活 agent 模型、`activeSessionId` 事件过滤、协议事件缺 session 维度。详细能力与风险已写入 `MEMORY.md`。
- **实施完成（12 文件，+567/-179）**：bridge 改 agent 池（LRU=5、new_chat 仅 detach、stop 带会话、session_status 推送、事件全带 session 标签）；UI 会话路由 + runningIds + 历史列表脉冲动效 + 切回接续流式（resumeStreaming）；`patchPathHealthy` 加内容比对（插件更新自动刷新 profile）。顺手修了 4 处 `↔` 乱码、`durableFlush` 无会话时炸进程、execute_command 不落盘。
- 顶部新增「N 个任务进行中」提示：header 标题右侧次级小字（`.dsh-running-hint`，0 时隐藏），数据源 `runningIds.size`，`renderStatus()` 统一刷新，`relocalize()` 走缓存计数重绘。构建 + 24 测试全绿。
- 验证：24 个单测全绿；临时 DSH_HOME 协议冒烟（mint/list/open/状态推送）通过；带真实凭据的流式池化验证通过（A 在 detach 后继续流式、B 并发完成、切回 A 完整重放、0 个未标记事件）。测试环境已清理（含凭据副本）。build/ 已更新，待用户重载插件验证。未提交 git。
